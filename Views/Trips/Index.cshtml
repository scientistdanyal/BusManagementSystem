@model IEnumerable<BusManagementSystem.Models.Trip>

@{
    ViewData["Title"] = "Trips";
}

<div class="page-header">
    <div class="container">
        <h1>Trips</h1>
        <p class="lead">Manage bus trips and schedules</p>
    </div>
</div>

<div class="container">
    @if (Context.Session.GetString(BusManagementSystem.Controllers.AdminController.AdminSessionKey) == "true")
    {
        <a asp-action="Create" class="btn-create">
            ➕ Create New Trip
        </a>
    }

    <div class="table-container">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.BusId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.RouteId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DepartureTime)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ArrivalTime)
                    </th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <strong>Bus #@Html.DisplayFor(modelItem => item.BusId)</strong>
                            </td>
                            <td>
                                Route #@Html.DisplayFor(modelItem => item.RouteId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DepartureTime)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ArrivalTime)
                            </td>
                            <td class="text-end">
                                <div class="action-buttons">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn-action btn-details">Details</a>
                                    @if (Context.Session.GetString(BusManagementSystem.Controllers.AdminController.AdminSessionKey) == "true")
                                    {
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn-action btn-edit">Edit</a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn-action btn-delete">Delete</a>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center empty-state">
                            <p class="mt-2" style="font-size: 48px;">📦</p>
                            <p class="mt-2">No trips found. <a asp-action="Create">Create a new trip</a> to get started.</p>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
